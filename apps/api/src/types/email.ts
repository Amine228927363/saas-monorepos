export interface shareEmail {
  email: string;
  full_name: string;
  role: string;
  link: string;
}
export interface contactEmail {
  name: string;
  email: string;
  subject?: string;
  message: string;
}
